const db = require('../models/index')

const getTopDoctorHome = (limit) => {
    return new Promise((resolve, reject) => {
        try {

        } catch (e) {
            reject(e);
        }
    })
}

module.exports = {
    getTopDoctorHome
}